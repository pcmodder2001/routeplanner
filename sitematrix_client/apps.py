from django.apps import AppConfig


class SitematrixClientConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "sitematrix_client"
    verbose_name = "SiteMatrix client"
